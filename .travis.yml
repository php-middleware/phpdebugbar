language: php

matrix:
  fast_finish: true
  include:
    - php: 5.5
    - php: 5.6
    - php: 7
    - php: 7.1
    - php: hhvm

install:
  - travis_retry composer install --no-interaction --prefer-source
  - composer info -i

script:
  - ./vendor/bin/phpunit
